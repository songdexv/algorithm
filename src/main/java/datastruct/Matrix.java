package datastruct;

/**
 * 矩阵操作
 * @author songdexu
 * @date 2019/5/14
 */
public class Matrix {
    /**
     * 沿对角线从左上角至右下角打印m*n阶矩阵
     * @param matrix
     */
    public void printMatrixByCrossLine(int[][] matrix) {
        int m = matrix.length;
        int n = matrix[0].length;
    }
}
